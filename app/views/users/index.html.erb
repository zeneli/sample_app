<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>
<ul class="users">
  <%= render @users %>   <!-- renders each user w/ _user.html.erb -->
  <!--<% @users.each do |user| %>
    <%= render user %>
   <% end %>-->
</ul>
<%= will_paginate %> 
